import type { LevelConfig } from "../LevelConfig";

const Level4Config: LevelConfig = {
  baseFallSpeed: 2,
  note: "it`s hell ...",
  pointsMultiplier: 4,
  heroHp: 1,
} as const;

export { Level4Config };
